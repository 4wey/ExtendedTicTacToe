# ExtendedTicTacToe

Графическая реализация игры **Крестики-нолики** на C++ с использованием **Qt 6**.  
Проект поддерживает несколько игровых режимов, включая классический и экспериментальный **Score Mode** с весами клеток и подсчётом очков.

---

## 📌 Возможности

- 🎮 Классический режим **3×3**
- 🧮 Score Mode (игра на очки)
- 🔀 Различные режимы заполнения поля (сверху вниз, слева направо и др.)
- 📊 Подсчёт очков и статистики
- 📜 Окно с правилами для каждого режима
- 👁 Возможность **показывать/скрывать веса клеток**
- 🖼 Qt GUI (виджеты, диалоги)
- 🧩 Расширяемая архитектура игровых режимов

---

## 🧱 Архитектура проекта

Проект намеренно разделён на логические модули, чтобы было удобно:
- добавлять новые режимы
- отлаживать логику
- изменять UI независимо от движка игры

TicTacToe/
├── game/ # Игровая логика (без UI)
│ ├── game_engine.* # Центральный игровой движок
│ ├── game_types.h # Общие enum'ы и типы
│ ├── modes/ # Игровые режимы
│ │ ├── igame_mode.h
│ │ ├── classic_mode.*
│ │ └── score_mode.*
│ └── score/ # Логика score-мода
│ └── score_helpers.*
│
├── widgets/ # Пользовательские виджеты
│ └── boardwidget.*
│
├── ui/ # Диалоги интерфейса
│ └── rulesdialog.*
│
├── mainwindow.* # Главное окно приложения
├── statsdialog.* # Диалог статистики
├── main.cpp # Точка входа
├── CMakeLists.txt
└── README.md


---

## 🎮 Игровые режимы

### Classic 3×3
- Классические правила крестиков-ноликов
- Побеждает игрок, собравший линию из 3 символов
- Без очков и весов

### Score Mode
Экспериментальный режим с расширенными правилами:

- Каждая клетка имеет **целочисленный вес**
- X и O имеют **разные стоимости ходов**
  - X — только чётные
  - O — только нечётные
- Очки считаются как сумма весов занятых клеток в линиях
- Возможны ограничения на ход:
  - только в активной строке
  - только в активном столбце
- Можно включить отображение весов клеток (для стратегии или отладки)

---

## 🧠 Основные концепции

- **IGameMode** — интерфейс игрового режима  
  Каждый режим (classic, score и будущие) реализует его
- **GameEngine** — не знает про UI, управляет состоянием игры
- **BoardWidget** — только отображение и ввод, без логики
- **UI ↔ Engine** — связаны через чёткие методы и структуры

---

## 🛠 Сборка проекта

### Требования
- macOS / Linux / Windows
- **Qt 6**
- **CMake ≥ 3.16**
- Компилятор с поддержкой C++17

### Сборка и запуск

Из корня проекта:

```bash
cmake -S . -B build
cmake --build build -j
./build/game
