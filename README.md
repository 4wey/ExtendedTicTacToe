# TicTacToe (Qt / C++)

Графическая реализация игры **Крестики-нолики** на C++ с использованием **Qt 6**.  
Проект поддерживает несколько игровых режимов, включая классический и экспериментальный **Score Mode** с весами клеток и подсчётом очков.

Проект создаётся как учебный и архитектурный, с упором на:
- чистое разделение логики и UI
- расширяемость режимов
- удобство отладки и дальнейшего развития

---

## 📌 Возможности

- 🎮 Классический режим **3×3**
- 🧮 Score Mode (игра на очки)
- 🔀 Различные режимы заполнения поля  
  (сверху-вниз, слева-направо и др.)
- 📊 Подсчёт очков и статистики
- 📜 Окно с правилами для каждого режима
- 👁 Возможность показывать / скрывать веса клеток
- 🖼 Qt GUI (Widgets)
- 🧩 Расширяемая архитектура игровых режимов

---

## 🧱 Архитектура проекта

Проект намеренно разделён на логические модули, чтобы было удобно:
- добавлять новые режимы
- отлаживать игровую логику
- изменять UI независимо от движка игры


---

## 🎮 Игровые режимы

### Classic 3×3
Классические правила крестиков-ноликов:

- поле 3×3
- X ходят первыми
- победа при сборе линии из 3 символов
- ничья при заполнении поля без победителя

---

### Score Mode
Экспериментальный режим с расширенными правилами и подсчётом очков.

Основные идеи:
- каждая клетка имеет **целочисленный вес**
- стоимость линии равна сумме весов занятых клеток
- X и O имеют разные стоимости ходов:
  - X — только чётные значения
  - O — только нечётные значения
- могут действовать ограничения на допустимые ходы:
  - только в активной строке
  - только в активном столбце
- победитель определяется по сумме очков, а не по линии

Дополнительно:
- можно включить отображение весов клеток
- режим подходит как для игры, так и для анализа стратегий

---

## 🧠 Основные концепции

- **IGameMode**  
  Абстрактный интерфейс игрового режима.  
  Каждый режим (classic, score и будущие) реализует его.

- **GameEngine**  
  Управляет состоянием игры и режимами.  
  Не зависит от UI.

- **BoardWidget**  
  Отвечает только за отображение поля и ввод пользователя.

- **UI ↔ Engine**  
  Связаны через чётко определённые структуры и методы, без логики в UI.

---

## 🛠 Сборка проекта

### Требования

- macOS / Linux / Windows
- **Qt 6**
- **CMake ≥ 3.16**

---

### Сборка и запуск

Из корня проекта:
bash
cmake -S . -B build
cmake --build build -j
./build/game
